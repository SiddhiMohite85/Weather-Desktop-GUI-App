Weather GUI App 🌦️

A simple desktop weather application built with Node.js and Electron.js.
It fetches real-time weather data from the OpenWeather API and displays it in a user-friendly GUI.
A cross-platform desktop weather application built with Electron.js and Node.js that displays real-time weather data fetched from the OpenWeatherMap API. It provides temperature, humidity, and wind speed details in a clean and responsive interface, packaged as a standalone executable for Windows.
